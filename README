#本项目是springboot工程集成UAC单点登录的一个demo
- 所有请求通过AuthInterceptor拦截器，请求中获取token，首次到UAC进行验证。
- 如果token为空则重定向请求到统一指定的登录界面，将本次请求的路径作为参数设置到重定向请求中。
- token验证通过后，将token以及用户信息及权限信息放入缓存中，下次请求将从缓存中获取。
- 业务系统需提供获取用户信息及权限信息的接口。